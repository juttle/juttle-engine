// Showcase juttle-viz views
//
// XXX add more views, only tile for now

(
emit -limit 10 | put value=count() | put name='apples';
emit -limit 10 | put value=count() | put name='bananas'
)
| view tile -facetFields ['name']

